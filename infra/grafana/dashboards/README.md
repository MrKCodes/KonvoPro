# Grafana Dashboards

Placeholder directory authored in task 1.7 (Phase 0). The four dashboards
required by Requirement 18.5 — **DM Health**, **Calls**, **Auth**, and
**System** — are authored in task 10.2 (Phase 9) once the api emits the
metrics named in design §19.1.

For now this directory exists so the compose file can mount it (or so a
later provisioning task can drop JSON dashboard exports here without a
follow-up `mkdir`).
