{
  "name": "@konvo/crypto",
  "version": "0.0.0",
  "private": true,
  "description": "libsignal wrapper for Konvo: identity, prekeys, sessions, ratchet, attachments, safety-numbers.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@noble/curves": "^1.6.0",
    "@noble/hashes": "^1.5.0"
  },
  "devDependencies": {
    "@konvo/protocol": "workspace:*",
    "@types/web": "^0.0.187",
    "fast-check": "^3.23.0",
    "typescript": "^5.6.3",
    "vitest": "^4.1.0"
  }
}
