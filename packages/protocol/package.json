{
  "name": "@konvo/protocol",
  "version": "0.0.0",
  "private": true,
  "description": "Shared wire-format types and msgpack codecs for Konvo (placeholder; expanded in task 3.1).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@msgpack/msgpack": "^3.1.3"
  },
  "devDependencies": {
    "fast-check": "^3.23.0",
    "typescript": "^5.6.3",
    "vitest": "^3.2.6"
  }
}
